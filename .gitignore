node_modules/
# Every .env variant, the example included — the variables and where to get
# them are documented in CLAUDE.md instead, so nothing env-shaped is tracked.
.env*
.DS_Store
Thumbs.db
*.log

dist/
.astro/

# Derived from the Google Calendar by scripts/fetch-calendar.mjs — refetched on
# every build, so never committed.
.cache/
public/images/gcal/
